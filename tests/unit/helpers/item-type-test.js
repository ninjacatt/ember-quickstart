
import { itemType } from 'ember-quickstart/helpers/item-type';
import { module, test } from 'qunit';

module('Unit | Helper | item type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemType([42]);
  assert.ok(result);
});

